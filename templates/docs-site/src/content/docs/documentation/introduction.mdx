---
title: Introduction
description: This project's docs site, running on Doctor.
---

import { Note } from '../../../components/mintlify';

This is the docs-site template. A real project's content and `nav.config.json` are written here by the build worker — this placeholder only shows when previewing the template on its own.

<Note>Callouts like this one match Mintlify's `Note`, `Info`, `Tip`, `Check`, `Warning`, and `Danger` tags.</Note>
