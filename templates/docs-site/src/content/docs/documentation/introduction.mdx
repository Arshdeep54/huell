---
title: Introduction
description: This project's docs site, running on Huell.
---

import { Note } from '../../../components/content';

This is the docs-site template. A real project's content and `nav.config.json` are written here by the build worker — this placeholder only shows when previewing the template on its own.

<Note>Callouts like this one use the `Note`, `Info`, `Tip`, `Check`, `Warning`, and `Danger` tags.</Note>
